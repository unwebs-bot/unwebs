{
  "name": "unwebs-theme",
  "version": "1.0.0",
  "private": true,
  "description": "Unwebs WordPress theme — dev tooling (BrowserSync)",
  "scripts": {
    "watch": "browser-sync start --config bs-config.cjs"
  },
  "devDependencies": {
    "browser-sync": "^3.0.2"
  }
}
